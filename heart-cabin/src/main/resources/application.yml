server:
  port: 8080

spring:
  # 数据库配置（本地MySQL）
  datasource:
    url: jdbc:mysql://localhost:3306/heart_cabin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.heartcabin.entity